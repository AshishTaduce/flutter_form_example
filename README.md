# Flutter Form Example

A demonstration app for the `flutter_form_generator` plugin. This app showcases various form configurations, field types, and validation scenarios.

## 📱 Features Demonstrated

-   **Basic Forms**: Simple login and registration forms.
-   **Complex Fields**: Date/Time pickers, File uploads, Searchable dropdowns.
-   **Validation**: Custom regex patterns for passwords and emails.
-   **Dependencies**: Fields that enable/disable based on other values.
-   **API Integration**: Mock submission actions.

## 🚀 Running the App

1.  Ensure you have the `flutter_form_generator` plugin available in the parent directory.
2.  Run the app:

```bash
flutter run
```

## 🧪 Testing New Features

The app includes a "Test New Features" section where you can paste JSON generated from the `flutter_form_builder` to verify its rendering and behavior in a native mobile environment.
